[user]
	name = cross
	email = cross@vgg.ru
[alias]
	co = checkout
        ci = commit
        st = status
        br = branch
       
        unstage = reset HEAD -- 
        ciback = commit --amend

        df = diff
        changes=diff --name-status -r
        diffstat=diff --stat -r        
        
        lg = log -p
	last = log -1 HEAD
        who = shortlog -s --
        hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short

        type = cat-file -t
        dump = cat-file -p
[core]
	editor = vim
	excludesfile = /home/cross/.gitignore_global
	autocrlf = input
[color]
	ui = true
[push]
	default = current
[branch]
	autosetuprebase = always
[pull]
	rebase = true
